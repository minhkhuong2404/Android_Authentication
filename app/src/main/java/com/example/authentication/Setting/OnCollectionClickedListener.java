package com.example.authentication.Setting;

import com.example.authentication.MyCourses.CollectionItem;

public interface OnCollectionClickedListener {
    void getSelected(CollectionItem collectionItem, int position);
}